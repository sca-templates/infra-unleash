---
description: Show the status of the Unleash stack containers.
agent: build
---

# PS

Run `make ps` from the repo root and report the container states (`unleash`
and `unleash-postgres` must be `healthy`).
