# Non-secret defaults — copy to .env or generate it from Vault (make env).
# Secret placeholders are filled by scripts/gen-env.sh; never commit .env.

ENVIRONMENT=dev

UNLEASH_PORT=4242
UNLEASH_ADMIN_USERNAME=admin
UNLEASH_ADMIN_PASSWORD=
UNLEASH_DB_PASSWORD=
